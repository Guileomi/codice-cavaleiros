package com.api.cavaleiros.repositorio;

import com.api.cavaleiros.dominio.AtributoLuta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AtributoLutaRepository extends JpaRepository<AtributoLuta, Long> {
}
